#include "main.h"
/**
 * add - function that adds two integers
 * @a: a parameter of add
 * @b: a parameter of add
 * Return: returns value of sum
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
